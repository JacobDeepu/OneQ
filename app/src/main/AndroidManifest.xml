<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jdream.oneq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneQ">
        <activity
            android:name=".business.MainBusinessActivity"
            android:label="@string/title_activity_main_business" />
        <activity
            android:name=".user.MainUserActivity"
            android:label="@string/title_activity_main_user" />
        <activity android:name=".business.RegistrationActivity" />
        <activity android:name=".user.RegistrationActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
    </application>

</manifest>